#include <stdio.h>
#include <conio.h>

main ()
{
	int i = 1, n;

	clrscr();

	printf("\n\n\tEnter any Number :");
	scanf("%d", &n);

	for (; i <= n; i++)
	{

		if (i % 2 != 0)
		{
			continue;
		}

		printf("%d.\t", i);
	}

	getch();
}