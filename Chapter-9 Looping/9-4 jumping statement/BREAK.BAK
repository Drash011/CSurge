#include <stdio.h>
#include <conio.h>

main ()
{
	int i = 1, n;

	clrscr();
	printf("\n\n\tEnter any Number :");
	scanf("%d", &n);

	while (i <= n)
	{
		if (i >= 10)
		{
			break;
		}
		printf("%d.\n", i);
		i++;
	}

	getch();
}