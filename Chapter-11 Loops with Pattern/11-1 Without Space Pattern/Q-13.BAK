#include <stdio.h>
#include <conio.h>

main ()
{
	int i, j;

	clrscr();
	printf("\n\n");
	for (i = 1; i <= 5; i++)
	{
		for (j = i; j <= 5; j++)
		{
			if (j % 2 == 1)
				printf("%c   ", 64 + j);
			else
				printf("%c   ", 96 + j);
		}
		printf("\n");
	}

	getch();
}